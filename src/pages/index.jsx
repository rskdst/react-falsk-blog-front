import React, { useState } from 'react';

const Home = (props) => {
    const [collapsed, setCollapsed] = useState(false);
    // @ts-ignore
    return (
        <div>
            首页
        </div>

    );
};

export default Home;